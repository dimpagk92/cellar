{
  "name": "@cellar/adapter-browser",
  "version": "0.1.0",
  "description": "CEL browser adapter — DOM-based context for web apps with shadow DOM, incremental updates, and network awareness",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --exclude '**/integration.test.ts' --exclude '**/node_modules/**'",
    "test:integration": "vitest run src/__tests__/integration.test.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@cellar/agent": "workspace:*",
    "playwright": "^1.59.1",
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.0"
  }
}
