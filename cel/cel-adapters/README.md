# Removed — use `cel-boot`

This crate was folded into [`cel/cel-boot`](../../cellar/cel/cel-boot) in the main repo (June 2026).

Use `cel_boot::register_default_adapters` instead of `cel_adapters::register_default_adapters`.

This OSS mirror copy is kept only for historical reference until the mirror sync completes.
