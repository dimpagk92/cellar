{
  "name": "@cellar/mcp-server",
  "version": "0.1.0",
  "description": "CEL MCP server — structured computer context for AI agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./server.js": "./dist/server.js"
  },
  "bin": {
    "cel-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "pnpm --filter @cellar/agent build && pnpm --filter @cellar/adapter-browser build && tsc",
    "test": "node ../tests/cortex/mcp-protocol.mjs",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@cellar/adapter-browser": "workspace:*",
    "@cellar/agent": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
